/* Minimal stub for MinGW cross-build (MS SDK ships this with WebView2 tooling). */
#ifndef EVENTTOKEN_H
#define EVENTTOKEN_H

typedef struct EventRegistrationToken {
	__int64 value;
} EventRegistrationToken;

#endif
